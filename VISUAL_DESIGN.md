# MoeNotes 统一视觉设计规范

> 状态：生效中 · 版本：1.0 · 更新日期：2026-07-11<br>
> 适用范围：所有 Astro 页面、React 交互组件、主题、图标、插图与后续新增功能

本文档是 MoeNotes 的视觉与交互约束。目标不是限制合理创新，而是确保不同页面和不同开发者产出的界面仍然属于同一个产品。

文中使用以下强度词：

- **必须**：合并前必须满足；不满足需要记录例外原因。
- **应**：默认遵守；只有明确的体验收益时才偏离。
- **禁止**：不得在新代码中出现。
- **可**：按内容与场景选择。

## 1. 规范来源与优先级

视觉实现按以下层级复用：

1. `src/styles/tokens.css`：浅色主题、字体和基础设计令牌的唯一值来源。
2. `src/styles/themes.css`：暗色主题令牌覆盖。
3. `src/styles/global.css`：全局基础样式、纸张/印章原语和通用交互。
4. `src/components/shared/`：Modal、Popover、Filter 等可复用交互组件。
5. `src/components/design-system/DesignSystemPage.tsx`：可运行的视觉样例，访问 `/tools/design-system` 查看。
6. 本文档：定义令牌和组件应在什么场景使用，以及交付验收标准。

当文档、样例和代码不一致时，不得在业务页面内临时复制一套新样式。应先确认期望方案，再在同一次变更中同步令牌、公共组件、设计系统样例和本文档。

## 2. 品牌视觉原则

MoeNotes 的核心气质是“温暖、清晰、轻手作感的资料手帐”。

- **纸张感**：以暖白纸面和低对比点阵背景建立氛围，避免纯白、纯黑和冷灰主导页面。
- **可读性优先**：装饰只能帮助建立层级，不能干扰正文、数据或操作。
- **轻触感**：描边、印章阴影和轻微位移表达可交互性；动效应短促、克制。
- **圆润但有结构**：容器使用大圆角，控件以胶囊和圆形为主，同时通过统一描边保持清晰边界。
- **主题等价**：浅色与暗色模式必须具有相同的信息层级、状态含义和可操作性。
- **多语言稳定**：视觉不能依赖某一种语言的字长；中文、英文、日文切换后不得破坏布局。

## 3. 颜色系统

### 3.1 语义令牌

业务组件必须使用 `--mn-*` 语义令牌，禁止直接写新的十六进制、RGB 或 HSL 颜色。透明遮罩、数据本身携带的颜色和第三方品牌色可作为例外，但必须集中定义并说明用途。

| 类别 | 令牌 | 浅色基准 | 用途 |
| --- | --- | --- | --- |
| 页面 | `--mn-bg` | `#FAF6F0` | 页面底色、深色文字上的反色内容 |
| 纸面 | `--mn-paper` | `#FFFBF5` | 卡片、弹窗、菜单、主要控件 |
| 表面 | `--mn-surface` | 半透明暖白 | 输入区、次级区域 |
| 强表面 | `--mn-surface-strong` | 高不透明暖白 | 需要更清晰分层的次级面板 |
| 正文 | `--mn-text` | `#2B2A27` | 标题、正文、主要图标 |
| 次正文 | `--mn-ink-soft` | `#4A4843` | 次要但仍需清晰阅读的内容 |
| 弱正文 | `--mn-text-muted` | `#8A857C` | 辅助说明、占位、非当前状态 |
| 边界 | `--mn-border` | `#2B2A27` | 卡片与控件轮廓 |
| 主强调 | `--mn-accent` | `#E87461` | 焦点、强调线、状态提示 |
| 深强调 | `--mn-accent-deep` | `#C85A47` | 主按钮、选中态、强调文字 |
| 弱强调 | `--mn-accent-soft` | `#FBE3DC` | 选中背景、轻提示背景 |
| 次强调 | `--mn-pink` / `--mn-cyan` / `--mn-amber` / `--mn-yellow` / `--mn-mint` | 见令牌文件 | 分类、标签、插图和有限的数据区分 |
| 危险/错误 | `--mn-rose` | `#B85C6B` | 错误、危险操作、失败状态 |

### 3.2 使用规则

- 一个视图必须以中性色为主体，主强调色只用于主要行动、选中状态和关键提示。
- 同一操作区域应只有一个视觉主行动；多个按钮不得同时使用主强调填充。
- 辅助色不得仅凭颜色传达业务含义，必须同时提供文字、图标、形状或位置线索。
- 文本应优先使用 `--mn-text`、`--mn-ink-soft`、`--mn-text-muted` 三层层级。
- 禁止使用 `--mn-bg` 作为任意彩色背景上的通用文字色；必须确认浅色和暗色主题下均有足够对比度。
- 暗色模式只能通过 `themes.css` 覆盖同名语义令牌，业务组件内禁止判断主题后切换硬编码颜色。

## 4. 字体与排版

### 4.1 字体角色

| 角色 | 令牌 | 使用场景 |
| --- | --- | --- |
| 展示字体 | `--mn-font-display` | 页面标题、区块标题、重要按钮、弹窗标题 |
| 正文字体 | `--mn-font-body` | 正文、表单、导航、数据与说明 |
| 手写字体 | `--mn-font-hand` | 少量眉题、便签式短句和装饰性标签 |
| 注记字体 | `--mn-font-note` | 非关键的短注释，不用于长正文 |

中文环境由 `data-locale="zh-CN"` 切换到霞鹜文楷系列；字体加载入口位于 `RootLayout.astro`。禁止在单个组件内重新声明字体栈。

### 4.2 排版层级

- Hero 标题：移动端 `text-5xl`，小屏及以上 `text-7xl`，大屏可到 `text-8xl`；仅首页或同等级品牌场景可用。
- 页面 H1：移动端 `text-4xl`，`sm` 起 `text-6xl`，紧凑行高约 `1.02`。
- 区块 H2：`text-2xl` 至 `text-5xl`，由区块重要性和断点决定。
- 卡片 H3：通常 `text-lg`，不应通过超大字号抢占页面标题层级。
- 正文：默认 `text-base`；长说明使用约 `leading-8`，行宽应控制在 `max-w-2xl` 至 `max-w-3xl`。
- UI 文本：默认 `text-sm`；元信息和标签可用 `text-xs`；禁止使用小于 `10px` 的可读文本。
- 字重：项目将 Tailwind `font-bold` 映射为 500、`font-black` 映射为 600。新组件应沿用该层级，不得通过 700–900 的超重字制造强调。
- 全大写只用于极短的拉丁标签；中文和日文不得强制 uppercase。

## 5. 间距、尺寸与栅格

- 采用 Tailwind 的 4px 基础尺度。优先使用 8、12、16、20、24、32、40、48px；禁止随意引入 13、17、29px 等无明确理由的间距。
- 页面主容器使用 `max-w-[120rem]` (1920px)，移动端水平内边距 16px，`md` 起通常为 24px。且支持 `3xl` (1600px)、`4xl` (1920px)、`5xl` (2200px) 的更大栅格扩展。
- 卡片内边距：移动端 16–24px，较大断点 24–32px；Hero 可放宽到 40–48px。
- 同组控件间距通常为 8–12px；卡片网格通常为 16–20px；主要区块间距通常为 32–48px。
- 内容布局必须移动端优先：默认单列，`sm` 可进入双列，`lg` 才进入三列或主次栏。
- 不得用固定宽度承载可变文本。需要固定尺寸的图标按钮、缩略图和装饰元素除外。
- 文本容器必须允许 `min-w-0`、换行或截断，避免英文长词和较长日文撑破布局。

项目断点包含 Tailwind 默认值 `sm` 640px、`md` 768px、`lg` 1024px、`xl` 1280px，并扩展了用于超宽屏的 `3xl` 1600px、`4xl` 1920px 和 `5xl` 2200px 断点以提升高分辨率内容密度。不得在组件里另建接近但不同的媒体查询。

## 6. 形状、边框与阴影

### 6.1 形状家族

- 页面级卡片、弹窗、侧栏：`rounded-3xl`。
- 卡片内分区、菜单、Popover：`rounded-2xl`。
- 按钮、输入框、标签、分段控制：`rounded-full`。
- 图标操作：圆形；方形操作仅在含义或内容形态明确要求时使用，并保持大圆角。

### 6.2 边框

- 纸张卡片和主要浮层默认使用 `1.5px solid var(--mn-border)`。
- 小型控件可使用 2px 边框以保持轮廓清晰。
- 同一区域不得混用多种无语义差别的边框粗细。
- 禁止用极浅边框代替层级；MoeNotes 的视觉特征依赖清晰的墨色轮廓。

### 6.3 阴影

| 令牌 | 使用场景 |
| --- | --- |
| `--mn-shadow` | 普通纸张面板、静态卡片 |
| `--mn-shadow-stamp-sm` | 小标签、小图标按钮、按压后状态 |
| `--mn-shadow-stamp` | 常规按钮、Popover、次级浮层 |
| `--mn-shadow-stamp-lg` | 页面重点卡片、Modal、移动侧栏 |

禁止在业务组件中发明新的常规阴影。特殊发光只可用于明确状态，并必须在浅色和暗色主题中验证。

## 7. 组件规范

### 7.1 表面与卡片

- 普通纸面优先复用 `.mn-paper`、`.mn-card` 或已有卡片组合。
- 重点内容可使用大阴影，但同一屏幕不应让所有卡片都成为最高层级。
- 装饰元素应使用绝对定位并设置 `aria-hidden="true"`，不得进入阅读顺序。

### 7.2 按钮

- 主按钮：`--mn-accent-deep` 填充、清晰边框、胶囊形，仅用于当前区域的首要行动。
- 次按钮：`--mn-paper` 或 `--mn-surface-strong` 背景、墨色文字和边框。
- 三级按钮：无阴影或弱背景，仅在 hover/选中时显示 `--mn-cream-deep`。
- 图标按钮必须有可访问名称；常规触控目标应至少 40px，移动端优先 44–48px。
- 圆形/方形按钮使用 Stamp 交互；胶囊按钮使用 Float 或轻按压交互。不得在同一按钮叠加两套运动语言。
- 破坏性操作必须使用 `--mn-rose` 语义，并在执行前提供清晰文案或确认。

### 7.3 表单

- 输入框使用胶囊形、纸面/表面背景、1.5–2px 边框和 `text-sm`。
- 每个输入必须有可关联的 `<label>`；placeholder 不能替代标签。
- focus 使用强调色边框或 ring，必须保留明显的键盘焦点。
- error 使用 `--mn-rose` 或现有错误语义，同时显示文字说明；不得只改变颜色。
- disabled 必须同时表现为不可操作、降低强调和正确的 HTML disabled 状态。
- Select、Checkbox、Toggle 优先扩展设计系统现有模式，不得用无语义的 `div` 模拟控件。

### 7.4 Modal 与 Popover

- 对话框必须复用 `src/components/shared/Modal.tsx`，以获得焦点圈定、Escape 关闭、滚动锁定、返回键处理和焦点恢复。
- 浮动菜单必须优先复用 `src/components/shared/Popover.tsx`，避免被父级 overflow 裁切。
- Modal 尺寸使用 `sm`、`md`、`lg`、`xl` 既有枚举，不新增近似宽度。
- 遮罩只用于建立浮层层级，不得以强模糊或高不透明度遮蔽上下文。

### 7.5 筛选、导航与空状态

- 筛选面板优先复用 `BaseFilters`、`FilterButton` 和 `QuickFilterButton`。
- 当前导航、已选筛选和普通 hover 必须有不同表现；选中态使用弱强调背景和深强调文字。
- 加载状态应保持布局稳定，优先使用与最终内容同尺寸的占位或明确进度文案。
- 空状态必须说明“为什么为空”和用户可采取的下一步；禁止只放插图或单独写“暂无数据”。
- 错误状态必须说明影响范围，并在可恢复时提供重试或返回路径。

## 8. 交互状态

所有可交互组件必须覆盖适用状态：

| 状态 | 最低要求 |
| --- | --- |
| Default | 层级、标签和点击范围明确 |
| Hover | 背景、阴影或轻位移变化；不得导致布局抖动 |
| Active / Pressed | 约 1px 位移或轻缩放，反馈短促 |
| Focus visible | 2px 强调色轮廓或等价 ring，offset 约 3px |
| Selected / Current | 除颜色外还应有背景、描边、图标或 `aria-current` / `aria-pressed` |
| Disabled | 使用原生 disabled、不可点击、视觉弱化 |
| Loading | 禁止重复提交，并用文案或进度图形说明 |
| Error | 错误色加文字说明，必要时关联 `aria-describedby` |

不得移除 outline 后不提供等价的 `:focus-visible` 样式。新增通用按钮应优先复用 `.mn-focus`。

## 9. 图标与装饰语言

- 功能图标使用简洁的线性 SVG，推荐 `stroke-width: 2–2.5`、圆角端点，并通过 `currentColor` 跟随文本状态。
- 纯装饰图标必须 `aria-hidden="true"`；无文字的功能图标必须给按钮添加本地化 `aria-label`。
- 禁止用 Emoji 充当产品图标、状态图标或装饰资产；仓库已有 `lint:emoji` 检查。
- 可用装饰包括 `.mn-halftone`、`.mn-staff-bg`、`.mn-stripes-warm`、`.mn-stripes-cream`、`.mn-tape` 和 `.mn-hand-underline`。
- 单个卡片通常只使用一种装饰语言；装饰面积应显著小于内容面积，且不得降低文本对比度。
- 手写字体、胶带、旋转和 wobble 只用于少量强调，不应用于正文、表格或高频操作。

## 10. 动效

项目动效基于 `framer-motion` 和 `src/lib/animation/use-animation.ts`。

- 常规 CSS 状态过渡使用约 150ms。
- Spring 基线：`stiffness: 280`、`damping: 18`；Modal 使用 `stiffness: 300`、`damping: 28`。
- Stamp：圆形/方形操作，hover 轻微上浮或按压阴影变化，tap 轻缩放。
- Float：胶囊操作，hover 最多上浮 4px，tap 缩放到约 0.96。
- 列表进入可使用 60ms stagger；不得为长列表的每一项持续播放复杂动画。
- 禁止用动画改变信息顺序、阻塞操作或制造大幅页面位移。
- 自动循环动画必须可忽略且不承载必要信息；跑马灯等持续动画应允许暂停。
- 新增动效必须为 `prefers-reduced-motion: reduce` 提供关闭或显著减弱方案。

当前 `useSpringAnimation` 尚未接入系统的 reduced-motion 偏好，这是待偿还的可访问性债务，不应作为新增动效忽略该偏好的先例。

## 11. 响应式与国际化

- 从 320–375px 宽度开始设计，逐级增强到平板和桌面。
- `md` 起侧栏可占据约 18rem；主内容必须使用现有 CSS 变量偏移，不得重复硬编码另一套壳层尺寸。
- 固定浮动操作必须检查与底部安全区、滚动按钮、侧栏和 Modal 的碰撞。
- 横向标签栏在小屏应允许滚动或换行，不得压缩到不可点击。
- 文案必须进入 `src/i18n/messages/`；禁止在可见 UI 和 `aria-label` 中新增硬编码业务文本。
- 使用真实的中、英、日文案验证，而不是只用短占位词。按钮不得依赖固定字符数保持尺寸。
- 数据表或密集内容在移动端应选择横向滚动、卡片化或信息折叠，不得简单缩小字体。

## 12. 无障碍底线

- 使用语义化元素：操作用 `button`，导航用 `a`，标题按层级排列，表单使用 label。
- 所有功能必须可用键盘完成；弹层必须支持 Escape、焦点圈定和关闭后焦点恢复。
- 正文和关键控件应满足 WCAG AA 对比度；弱文本不得承担关键说明。
- 点击目标应留有足够间距，避免相邻小按钮误触。
- 动态结果、错误和加载完成提示应在需要时使用适当 live region。
- 200% 缩放时不得丢失内容或操作；不要锁死容器高度承载可变文本。
- 颜色、位置、动效和图标均不能成为唯一的信息表达方式。

## 13. 推荐实现方式

优先使用令牌和既有视觉原语：

```tsx
<button
  type="button"
  className="mn-focus mn-stamp-press rounded-full border-[1.5px]
    border-[var(--mn-border)] bg-[var(--mn-accent-deep)] px-6 py-3
    text-sm font-bold text-[var(--mn-paper)] shadow-[var(--mn-shadow-stamp)]"
>
  Primary action
</button>
```

禁止在业务组件内创建孤立风格：

```tsx
// 不允许：硬编码品牌色、任意圆角、任意阴影、缺少 focus 状态
<button className="rounded-[13px] bg-[#ff684f] shadow-xl">
  Action
</button>
```

当同一种样式出现三次，或需要处理三种以上状态时，应提取为公共组件或全局原语，而不是继续复制 Tailwind 类串。

## 14. 交付检查清单

每个新增或明显改版的界面在合并前必须检查：

- [ ] 颜色只使用既有语义令牌，浅色和暗色模式均通过。
- [ ] 页面在 375、768、1024、1440px 宽度下无溢出和遮挡。
- [ ] 中文、英文、日文真实文案下布局稳定。
- [ ] 标题层级、正文行宽、字号和字重符合排版规范。
- [ ] Default、hover、active、focus、disabled、loading、empty、error 中适用的状态完整。
- [ ] 键盘可完成全部操作，焦点清晰，Modal/Popover 行为正确。
- [ ] 图标和装饰具有正确的 `aria-label` 或 `aria-hidden`。
- [ ] 动效不阻塞任务，并为 reduced motion 提供降级。
- [ ] 优先复用了 `src/components/shared/` 和全局 `.mn-*` 原语。
- [ ] 设计系统页面已补充新的通用模式或变体。
- [ ] `bun run lint`、`bun run check` 和 `bun run build` 通过。

## 15. 例外与演进

确需偏离规范时，变更说明必须包含：使用场景、不能复用现有方案的原因、浅色/暗色/响应式/无障碍验证结果，以及是否应沉淀为新令牌或公共组件。

新增全局颜色、字体、阴影、圆角、断点或动效曲线，应先修改基础令牌或公共实现，再更新设计系统页面和本文档。业务页面不得成为新视觉规则的唯一来源。
