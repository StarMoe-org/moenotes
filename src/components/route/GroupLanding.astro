---
import type { AppLocale } from "@/config/locales";
import { t } from "@/i18n";
import { localizePath } from "@/i18n/routing";
import { getNavChildren } from "@/lib/route/registry";
import type { AppRoute } from "@/types/route";

interface Props {
  locale: AppLocale;
  group: AppRoute;
}

const { locale, group } = Astro.props;
const children = getNavChildren(group);
---

<section class="rounded-3xl border-[1.5px] border-[var(--mn-border)] bg-[var(--mn-paper)] p-6 shadow-[var(--mn-shadow-stamp-lg)] sm:p-8">
  {t(locale, "page.groupIntro") ? (
    <p class="mb-6 max-w-3xl font-medium leading-8 text-[var(--mn-text-muted)]">{t(locale, "page.groupIntro")}</p>
  ) : null}
  <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
    {children.length === 0 ? (
      <article class="relative rounded-2xl border-[1.5px] border-[var(--mn-border)] bg-[var(--mn-surface-strong)] p-6 shadow-[var(--mn-shadow-stamp)]">
        <h2 class="text-lg font-black text-[var(--mn-text)]">{t(locale, "page.placeholderTitle")}</h2>
        <p class="mt-2 text-sm font-medium leading-7 text-[var(--mn-text-muted)]">{t(locale, "page.placeholderDescription")}</p>
      </article>
    ) : children.map((item) => (
      <a href={localizePath(item.path, locale)} class="mn-list-card group relative rounded-2xl border-[1.5px] border-[var(--mn-border)] bg-[var(--mn-surface-strong)] p-6 shadow-[var(--mn-shadow-stamp)] transition hover:-translate-y-1 hover:bg-[var(--mn-cream-deep)]">
        <h2 class="text-lg font-black text-[var(--mn-text)] group-hover:text-[var(--mn-accent-deep)]">{t(locale, item.labelKey)}</h2>
        <p class="mt-2 text-sm font-medium leading-7 text-[var(--mn-text-muted)]">{t(locale, item.seo.descriptionKey)}</p>
        <span class="mt-5 inline-flex items-center gap-2 rounded-full border-2 border-[var(--mn-border)] bg-[var(--mn-accent-soft)] px-4 py-1.5 text-sm font-black text-[var(--mn-accent-deep)] shadow-[var(--mn-shadow-stamp-sm)]">
          {t(locale, "actions.open")}
          <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14" /><path d="m13 6 6 6-6 6" /></svg>
        </span>
      </a>
    ))}
  </div>
</section>
