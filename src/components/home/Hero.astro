---
import type { AppLocale } from "@/config/locales";
import { t } from "@/i18n";
import { localizePath } from "@/i18n/routing";
import { getRoutePathById } from "@/lib/route/registry";

interface Props {
  locale: AppLocale;
}

const { locale } = Astro.props;
const features = ["i18n", "breadcrumb", "masterdata", "settings", "scroll", "seo"] as const;
const databasePath = getRoutePathById("database");
const aboutPath = getRoutePathById("community");
---

<section class="mn-staff-bg relative overflow-hidden rounded-md border-[2.5px] border-[var(--mn-border)] bg-[var(--mn-paper)] p-6 shadow-[var(--mn-shadow-stamp-lg)] sm:p-10 lg:p-12">
  <div class="mn-halftone absolute right-6 top-6 h-32 w-32 text-[var(--mn-accent)] opacity-20"></div>
  <div class="mn-stripes-warm absolute -right-12 bottom-8 h-20 w-48 rotate-[-8deg] border-2 border-[var(--mn-border)] opacity-90"></div>
  <div class="absolute left-[55%] top-12 hidden h-28 w-28 rotate-6 rounded-md border-[2.5px] border-[var(--mn-border)] bg-[var(--mn-yellow)] shadow-[var(--mn-shadow-stamp)] lg:block"></div>
  <div class="relative max-w-4xl">
    <p class="mb-3 font-[var(--mn-font-hand)] text-2xl font-bold text-[var(--mn-accent)]">{t(locale, "home.eyebrow")}</p>
    <h1 class="max-w-3xl font-[var(--mn-font-display)] text-5xl leading-[0.95] tracking-tight text-[var(--mn-text)] sm:text-7xl lg:text-8xl">
      <span class="mn-hand-underline">{t(locale, "home.title")}</span>
    </h1>
    <p class="mt-6 max-w-2xl text-xl font-black text-[var(--mn-text)]">{t(locale, "home.subtitle")}</p>
    <p class="mt-4 max-w-2xl text-base font-medium leading-8 text-[var(--mn-text-muted)]">{t(locale, "home.description")}</p>
    <div class="mt-8 flex flex-wrap gap-3">
      <a href={localizePath(databasePath, locale)} class="mn-stamp rounded px-5 py-3 text-sm transition hover:-rotate-2">{t(locale, "home.ctaPrimary")}</a>
      <a href={localizePath(aboutPath, locale)} class="rounded border-2 border-[var(--mn-border)] bg-[var(--mn-surface-strong)] px-5 py-3 text-sm font-black text-[var(--mn-text)] shadow-[var(--mn-shadow-stamp-sm)] hover:bg-[var(--mn-cream-deep)]">{t(locale, "home.ctaSecondary")}</a>
    </div>
  </div>
</section>

<section class="mt-12">
  <div class="mb-6 flex flex-wrap items-end justify-between gap-3">
    <div>
      <p class="font-[var(--mn-font-hand)] text-xl font-bold text-[var(--mn-accent)]">Foundation notes</p>
      <h2 class="font-[var(--mn-font-display)] text-3xl tracking-tight text-[var(--mn-text)] sm:text-5xl">{t(locale, "home.featuresTitle")}</h2>
    </div>
  </div>
  <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
    {features.map((feature, index) => (
      <article class:list={["mn-tape relative rounded-md border-[2.5px] border-[var(--mn-border)] bg-[var(--mn-paper)] p-5 shadow-[var(--mn-shadow-stamp-lg)] transition hover:-translate-y-1", index % 2 === 0 ? "rotate-[-1deg]" : "rotate-[1deg]"]}>
        <div class="mb-4 grid h-11 w-11 place-items-center rounded border-2 border-[var(--mn-border)] bg-[var(--mn-accent)] text-[var(--mn-bg)] shadow-[var(--mn-shadow-stamp-sm)]">
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M12 2.75 14.35 9.65 21.25 12 14.35 14.35 12 21.25 9.65 14.35 2.75 12 9.65 9.65Z" />
          </svg>
        </div>
        <h3 class="text-lg font-black text-[var(--mn-text)]">{t(locale, `home.features.${feature}`)}</h3>
      </article>
    ))}
  </div>
</section>
