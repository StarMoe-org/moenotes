---
import CardDetail from "@/components/cards/CardDetail";
import PageLayout from "@/layouts/PageLayout.astro";
import type { AppLocale } from "@/config/locales";
import { getBuildCardDetail } from "@/lib/masterdata/build-data";

interface Props {
  locale: AppLocale;
  cardId: number;
}

const { locale, cardId } = Astro.props;
const initialData = await getBuildCardDetail(locale, cardId);
---

<PageLayout locale={locale} titleKey="seo.cardDetail.title" descriptionKey="seo.cardDetail.description">
  <CardDetail client:load locale={locale} cardId={cardId} initialData={initialData} />
</PageLayout>
