---
import { LOCALE_LABELS, SUPPORTED_LOCALES, type AppLocale } from "@/config/locales";
import { switchLocalePath } from "@/i18n/routing";

interface Props {
  locale: AppLocale;
  pathname: string;
}

const { locale, pathname } = Astro.props;
---

<div class="flex items-center gap-1 rounded-full border-2 border-[var(--mn-border)] bg-[var(--mn-paper)] p-1 text-xs font-bold shadow-[var(--mn-shadow-stamp-sm)]">
  {SUPPORTED_LOCALES.map((item) => (
    <a
      href={switchLocalePath(pathname, item)}
      class:list={[
        "rounded-full px-2.5 py-1 transition",
        item === locale ? "bg-[var(--mn-accent-soft)] text-[var(--mn-accent-deep)]" : "text-[var(--mn-text-muted)] hover:bg-[var(--mn-cream-deep)] hover:text-[var(--mn-text)]",
      ]}
    >
      {LOCALE_LABELS[item]}
    </a>
  ))}
</div>
