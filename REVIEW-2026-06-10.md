# Moenotes 基模审阅报告

> 审阅日期：2026-06-10 · 审阅范围：全部源码（src/ 约 6400 行 + scripts/ 工程化脚本 + 配置）
> 项目定位：Bangdream Ournotes（未上线）的类 Bestdori 资料站基模，数据层为占位骨架

---

## 一、健康度实测（非静态阅读，均实际运行验证）

| 检查项 | 结果 |
|---|---|
| `astro build` | ✅ 通过，45 页全部生成（新增 cards 静态页；动态详情 pattern 预留但无占位产物） |
| `bun run lint`（6 个自研脚本） | 全部通过 |
| `astro check` | 0 errors / 0 warnings / 2 hints（use-animation.ts 自引用 @deprecated 成员） |
| 三语言 i18n key 一致性 | ✅ zh/ja/en 各 205 个 key，脚本展平比对完全一致 |
| dist 产物抽查（canonical/hreflang/JSON-LD/robots/sitemap） | 输出结构正确，细节问题见下文 SEO 节 |

**总体结论：基模质量明显高于平均水平。** 路由注册表中心化驱动、缓存降级纪律、防 FOUC 内联脚本、自研 lint 门禁等设计都是对的。主要风险集中在四处：**① masterdata 缓存失效链路断裂（唯一功能性高危 bug）；② 注册表没有为动态详情页（cards/characters/songs——本站未来主体）建模；③ 浮层横切层（ESC/滚动锁/焦点）多套实现叠加且可访问性系统性缺失；④ 大量"定义了但未接线"的悬空配置。** ①② 应在填充真实数据之前解决，否则第一个详情页接入时将被迫大改。

---

## 二、高严重度（必须修）

### H1. masterdata 版本一旦写入就永不刷新，缓存永不失效 ✅ 已修复
`src/lib/masterdata/client.ts:8` + `src/lib/masterdata/version.ts:13-19`

> ✅ 2026-06-10 修复：`fetchMasterData` 每次优先请求 version manifest，localStorage 版本仅作为网络失败降级；manifest 官方/镜像互备，失败不再污染 `mock-0`，并支持 stale-on-error。验证：`bun run lint`、`bun run check`、`bun run build`。

`fetchMasterData` 取版本用 `getStoredDataVersion() ?? (await fetchVersionManifest(...))`，而 `setStoredDataVersion` 只在 `fetchVersionManifest` 成功时调用，后者又只在 localStorage 无版本时被调用——首次成功后版本被永久钉死，之后所有会话 `entry.version === version` 恒成立，**游戏数据更新后用户永远拿不到新数据**。这是资料站的命脉逻辑。

**建议**：反转优先级——每次会话（或带 TTL）请求 version manifest，localStorage 版本仅作网络失败时的降级值。

### H2. 路由注册表没有动态路由的任何概念，详情页扩展路径不通 ✅ 已修复
`src/pages/[...locale]/index.astro:31-43` + `src/lib/route/static-paths.ts` + `src/lib/seo/metadata.ts:22-39`

> ✅ 2026-06-10 修复：`AppRoute` 增加 `kind/pattern/component/staticParams`，注册 `cards/characters/music` 三类详情 pattern；catch-all 改为 registry 组件出口，`getStaticPaths` 通过 props 下传 match/params/meta/detail，Layout 支持逐页 metadata 与 breadcrumb detail。验证：`bun run lint:routes` 显示 14 static routes / 3 dynamic routes / 44 localized static paths；`bun run build` 生成 45 页。

- 所有 ja/en 页面由一个 catch-all 承载，"路由 → 组件"映射是硬编码三元链（`id === "home" ? ... : id === "design-system" ? ...`），加详情页只能继续堆分支，或新建独立页面文件但只覆盖无前缀 zh、`/ja/cards/1` 仍需手工接线（每个 section 双套路由）。
- Layout 只接受 `locale + pathname`，**没有逐页 metadata/og 图覆盖通道**；`buildBreadcrumbs` 支持 `detail` 参数但无 prop 能传入，未来详情页的 JSON-LD 面包屑末项会停在列表页。

**建议**：在 `AppRoute` 增加 `kind: "static" | "dynamic"`、`pattern`（如 `/cards/:id`）与组件映射表；`getStaticPaths` 经 `props` 下传 match/实体数据；Layout 增加 `meta?: Partial<PageMetadata>` 与 `breadcrumbDetail?` 透传。**这是基模阶段最该补的口子。**

### H3. 多 Modal 共享同一个 body-scroll-lock key ✅ 已修复
`src/components/shared/Modal.tsx:49`

> ✅ 2026-06-10 修复：`Modal` 改用 `useId()` 生成实例级 scroll-lock key，并维护 modal stack，避免多弹窗互相提前解锁。验证：`bun run check`、`bun run build`。

所有 Modal 实例都用 `lockBodyScroll("modal")`，Set 去重导致两个 Modal 同开时（如筛选 Modal 开着再按 `mod+,` 开设置）先关的那个会解锁 body，第二个还开着。keyed 锁的嵌套设计被共享 key 击穿。**建议**：每实例 `useId()` 作 key。

### H4. 浮层可访问性系统性缺失 ✅ 已修复
`Modal.tsx:90-141`、`CommandPalette.tsx:42-89`、`KeyboardShortcutsHelp.tsx`、`Sidebar.tsx:95-118`

> ✅ 2026-06-10 修复：`Modal` 统一补 `role="dialog"/aria-modal/aria-labelledby`、focus trap、初始焦点和关闭后焦点归还；CommandPalette 补 dialog/listbox/option ARIA、body scroll lock 与键盘导航；ShortcutsHelp 改用统一 Modal；移动 Sidebar 补 focus trap/焦点归还。验证：`bun run check`、`bun run build`。

四类浮层**全部没有 focus trap、没有关闭后焦点归还**；除移动 Sidebar 外连 `role="dialog"` / `aria-modal` / `aria-labelledby` 都没有。键盘用户 Tab 穿透遮罩、读屏用户感知不到模态边界。这是 shell 级横切缺陷，每个未来页面都会继承。**建议**：在 Modal 统一实现一次（trap + 初始焦点 + 焦点归还 + aria），CommandPalette/Help 对齐同一套。

---

## 三、中严重度

### 数据与缓存层
- **[M1] manifest 失败静默降级 `mock-0` 且无源互备** ✅ 已修复（`version.ts:21-33`）：manifest 已支持 official/mirror 回退；无本地版本时抛错，有本地版本时返回 `isFallback`，并由 client 配合 stale-on-error。
- **[M2] 用户设置 `masterdataSource`/`assetSource` 完全未接线且默认值矛盾** ✅ 已修复（`config/settings.ts:12` vs `masterdata/client.ts:7`）：masterdata/asset source 均读取 settings，`masterdataCacheBypass` 已被 client 消费；asset backup 会回退 main。
- **[M3] 资产缓存写放大** ✅ 已缓解（`asset-cache.ts:52-117`）：命中 touch 增加 60s 节流，写入后的 prune 改为 1s 防抖调度，避免单页多资源触发连续写事务/全表扫描。
- **[M4] `cached-fetch.ts:48-56` 原始 Response body 永不消费** ✅ 已修复：缓存写入改为先消费 `response.blob()`，同一份 blob 用于写缓存和 replay Response，避免 clone/tee 长时间缓冲。
- **[M5] `source: "backup"` 时丢失回退链** ✅ 已修复（`assets/url.ts:18-21`）：fallback 顺序改为 preferred → alternate，`backup` 会回退 `main`。
- **[M6] masterdata 层缺并发去重 / 旧版本清理 / stale-on-error** ✅ 已修复（与 assets 层成熟度不对称）；`fetchMasterData` 已增加 in-flight 去重、stale-on-error 与 `validate?: (raw) => T` 钩子。
- **[M7] `indexed-db.ts:40-43` onblocked 即 reject 且泄漏连接** ✅ 已修复：`onblocked` 不再 reject/清空 promise，保持 open request pending，等待旧标签页释放后正常 `onsuccess`。
- **[M8] `use-settings.ts` 未用 `useSyncExternalStore`** ✅ 已修复：hook 改为 `useSyncExternalStore`；DOM apply 移到 settings store 的 emit 流程统一执行一次。

### UI 与交互层
- **[M9] ESC/关闭逻辑三轨并行** ✅ 已修复：OverlayHost ESC 已检查 `isComposing`；Modal 维护 stack，仅栈顶响应 ESC/back；CommandPalette/Sidebar 也忽略 IME composing。
- **[M10] 两套 overlay 机制并存**：overlay-store（单槽）与 Modal 本地 state 叠加，是 H3/M9 系列交叉 bug 的根源。建议明确分层：overlay-store 管"谁激活"，锁/ESC/history 由统一 overlay 层做一次。
- **[M11] `sidebarMode`、`enableCommandPalette`、`enableBreadcrumbDropdown` 是死设置** ✅ 已修复：SettingsDrawer 补全 UI；HeaderActions/Sidebar/OverlayHost/Breadcrumbs 分别消费 sidebarMode、command palette 与 breadcrumb dropdown。
- **[M12] CommandPalette 无键盘导航** ✅ 已修复：补 ↑/↓/Enter、`role="dialog/listbox/option"`、`aria-activedescendant`、body scroll lock、焦点归还与平台化快捷键提示。
- **[M13] 全站不读 `prefers-reduced-motion`** ✅ 已修复：未显式设置时按 `prefers-reduced-motion` 推导默认动画等级，CSS 补媒体查询并在 reduced motion 下关闭 smooth scroll/长动画。
- **[M14] `BaseFilters.tsx:98-178` 折叠头/Expand 条是带 onClick 的 div**：不可 Tab、无 Enter/Space、无 `aria-expanded`。
- **[M15] `FilterButton.tsx:36-40` 原始 transform 字符串与 framer-motion 冲突**：首次 hover 后装饰性 rotate 永久丢失。改用 FM 的 `rotate` 值。
- **[M16] `Header.astro:147-168` 170 行命令式内联脚本**：window resize 监听不移除（引入 View Transitions 即泄漏）、硬编码引用页面组件的 `#design-system-nav`（层级倒置）、JS 写 `main.style.paddingTop` 与 AppShell 类名双事实源。建议重构为 CSS 变量驱动（`--mn-header-bottom` 这条路是对的）。
- **[M17] 侧边栏状态四处写入**：bootstrap 脚本、HeaderActions、Sidebar 三个 effect 各自写 CSS 变量 + dataset + sessionStorage，靠两个自定义事件缝合，事实源不唯一；桌面侧边栏还有 SSR 闪烁（用户上次收起时，静态 HTML 固定渲染展开态，要等 hydration 才消失——可用 `html[data-sidebar="closed"]` CSS 一行解决）。建议做成与 overlay-store 同款的模块级 store。
- **[M18] 字体加载三连**：CSS 顶部 `@import` Google Fonts（render-blocking）+ 同时引入 LXGW WenKai web/screen **两套全量中文字体**。建议 `<link rel="preconnect">` + head link、两套二选一。另外 `AppShell.astro:28-41` 七个 island 全部 `client:load`，CommandPalette/SettingsDrawer/Help/OverlayHost/ScrollToTop 可降 `client:idle`，避免 framer-motion 进首屏关键路径。

### SEO
- **[M19] canonical/sitemap 无尾斜杠 vs `build.format: "directory"`** ✅ 已修复：`astro.config.mjs` 设置 `trailingSlash: "always"`，canonical/hreflang/sitemap 使用带尾斜杠页面 URL。
- **[M20] `twitter:card=summary_large_image` 但从未输出 og:image** ✅ 已修复：`PageMetadata.image` 默认使用 `siteConfig.ogImage`，RootLayout 输出 `og:image` 与 `twitter:image`。
- **[M21] hreflang 缺 `x-default`** ✅ 已修复：RootLayout 为每页输出默认中文路径的 `hreflang="x-default"`。
- **[M22] JSON-LD `set:html={JSON.stringify(...)}` 不转义 `</script>`** ✅ 已修复：新增 `serializeJsonLd()`，所有 JSON-LD 输出统一 `.replace(/</g, "\\u003c")`。

### 工程化
- **[M23] `check-route-registry.mjs:33-37` 不变量检查恒真** ✅ 已修复：删除恒真检查，静态/动态 route pattern 分开计数，`lint:routes` 现在输出 14 static routes / 3 dynamic routes / 44 localized static paths。
- **[M24] lint 脚本"正则对拍源码"整体偏脆**：`check-i18n-keys` 正则改写 TS 丢 vm 执行（模板字符串/引用常量即炸）；`check-i18n-routing` 与 locales.ts 书写格式强耦合（键加引号即误报）；`check-architecture` 漏模板字符串路由字面量（`` `/cards/${id}` `` 恰是最想拦的）、注释误报；`scan-emoji` cwd 相对路径 + 静默吞错（换目录跑会假通过）且范围漏 ⭐⭕™ 和 U+FE0F。建议借 bun 原生 TS import 重写为真实模块级比对。
- **[M25] 协作基础设施缺位**：无 README、LICENSE（字体脚本派生自 OFL 的 Nowar Rounded，woff2 入库分发需附许可证）、CI、测试框架、通用 linter/formatter；python 字体工具链零依赖声明（无 requirements.txt，新协作者必失败）。
- **[M26] `@astrojs/tailwind` 是死依赖**（实际用的是 `@tailwindcss/vite`，且它面向 Tailwind 3）；字体子集管线是孤儿（输出 `public/fonts/` 但字体栈无 "Moenotes Rounded"、无 @font-face 引用——"已建好但未通电"）。

---

## 四、低严重度（选摘）

- `astro.config.mjs:11` vite alias 用 `URL.pathname`，Windows 上为 `/D:/...`，含空格/非 ASCII 路径会百分号编码；该 alias 与 tsconfig paths 冗余，建议 `fileURLToPath` 或直接删除。
- astro.config 的 i18n 块实际未被使用（无任何 `astro:i18n` 导入，路由全靠自定义 catch-all），与 `locales.ts` 双份配置需手工同步，易误导后人。
- `messages/*.ts` 的 `satisfies MessageTree` 不提供 key 对齐保障（仅 index signature），建议 ja/en 改 `satisfies DeepShapeOf<typeof zhCN>` 把保障升到编译期。
- ✅ `apply-theme.ts:26-28` bootstrap 对 localStorage 值为字符串 `"null"` 时整体失效已修；`colorScheme: "system"` 已通过 settings store 监听 `matchMedia` change。
- 存储 key `"moenotes:settings"` 三处重复定义（常量 ×2 + bootstrap 脚本字符串硬编码）。
- ✅ `cached-fetch.ts`：已移除缓存回放的 `content-encoding`，跳过 204/205/304 等 null-body 状态，并处理 `cache: "no-cache"` 为刷新语义。
- ✅ `masterdata/client.ts:13` 缓存命中 truthiness 判断已修：client 改用 cache entry 是否存在判断，合法 falsy 数据不再被当 miss。
- ✅ `shortcuts/manager.ts:42-55` 序列状态机已修：序列中未命中第二键会立即清空 pending。
- `community` 组 path 为 `/about` 且 `children: []`，渲染空 GroupLanding 网格；zh 首页与 ja/en 首页双渲染路径；`getLocalePrefix`、`BuildBreadcrumbOptions`、`SETTINGS_CHANGED_EVENT`（只 dispatch 无 listener）、IDB 索引等多处死代码。
- 共享组件硬编码英文文案/aria-label（"Close"/"Filter"/"Scroll to top"等），与 i18n 贯穿到 JSON-LD 的体系不一致；`HeaderActions` 搜索按钮硬编码 "⌘K"（Windows 用户看 Mac 符号）且小屏无 aria-label。
- `global.css:208` 用 `!important` 把 `.font-black` 改成 600，篡改 Tailwind 语义（应在 `@theme` 重定义）；暗色主题漏网：点阵背景墨点、`.mn-tape` 边框、`.mn-hand-underline` SVG 描边均硬编码亮色值。
- ✅ sitemap 已补 `xhtml:link` 多语言互链、`lastmod` 与 XML 实体转义。
- `dev` 与 `dev:fresh` 脚本完全相同，且每次 dev 都 `--force` 全量重建预构建，启动变慢；`typescript`/`@astrojs/check` 应移入 devDependencies；.gitignore 缺 `__pycache__/`。
- ✅ ScrollRestorer 已设置 `history.scrollRestoration = "manual"`，并将点击保存限制到 `closest("a[href]")`；同时接入 `enableScrollMemory` 设置。

---

## 五、优点（确认过的，不是客套）

- **路由注册表中心化**：单一 registry 驱动导航、面包屑、命令面板、SEO、sitemap、静态路径，`as const satisfies` 用法到位；locale 帮助函数基于 segment 解析（避开 `/japan` 误判），`localizePath` 幂等、strip/localize 对称，所有核对过的边界行为正确。
- **缓存降级纪律**：`safe-storage` 全套兜隐私模式/配额满；IDB 不可用时所有缓存函数静默 no-op，故障不打断请求主链路；assets 缓存有 TTL+stale 双窗口、LRU 双上限、体积上限、content-type 白名单、in-flight 去重、set-cookie 剔除、authorization/range 不缓存——这些细节多数同类项目都会漏。
- **settings 链路健壮**：逐字段白名单 normalize（损坏数据恢复默认）、跨标签页 storage 同步、防 FOUC 内联 bootstrap（自带枚举校验）一次性解决暗色与侧边栏偏移两个经典闪烁。
- **类型纪律**：`noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`，严格度高于多数项目；astro check 零错误。
- **自研 lint 门禁**：i18n 键、路由注册表、架构边界（禁裸 fetch/localStorage/硬编码路由）、文案外置、emoji 禁用全部纳入检查且通过；报告路径在 Windows 上做了归一化。
- **island 边界意识**：静态壳 + 精确 hydration，named slot 注入静态 Header 是教科书做法；token 体系（tokens → themes 两层、暗色逐色调校而非反色、`data-locale` 切字体栈）相当规范。
- **字体子集脚本**：OFL 保留名重命名、GB2312+JIS 覆盖、safelist、缓存，体现对 CJK web 字体的真实理解。

---

## 六、修复路线图（建议顺序）

**第一批 —— 填充真实数据之前必须完成**
1. ✅ H1 masterdata 版本失效（命脉 bug）
2. ✅ H2 动态路由建模 + Layout metadata/breadcrumb 透传口子
3. ✅ M2/M11 接线或删除全部"假设置"（source 切换、sidebarMode 等）——趁现在决定，避免债务化
4. ✅ M6 `fetchMasterData` 预留 validate 钩子

**第二批 —— 浮层层收敛（趁组件少，一次做对）**
5. ✅ H3/H4/M9/M12：Modal 统一实现锁(useId key)/ESC(栈顶+isComposing)/focus trap/aria，CommandPalette 补键盘导航（M10 overlay-store 唯一调度层仍保留为后续重构项）
6. ✅ M13 接入 prefers-reduced-motion

**第三批 —— 上线前一小时能修完但不修就持续漏分**
7. ✅ M19-M22 SEO 四件套（尾斜杠对齐、x-default、og:image、JSON-LD 转义）
8. ✅ M3/M4/M5 缓存性能与回退链

**第四批 —— 协作/开源前**
9. M25 README + LICENSE + CI（`bun run lint && bun run check`）+ python 依赖声明
10. ✅ M23 route registry lint 恒真检查已修；M24/M26（lint 全量重写、死依赖/字体管线）仍待后续处理

---

*审阅方式：4 个并行审阅代理逐文件通读全部源码，构建/lint/类型检查实际运行验证，dist 产物抽查；所有发现均经代码或产物确认，无猜测项。*
